import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    modalContent: {
        padding: 20,
        alignItems: 'center',
      },
      modalText: {
        fontSize: 24,
        marginBottom: 10,
      },
      emailText: {
        fontSize: 16,
        textDecorationLine: 'none',
      },
});
