import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  textButton: {
    minHeight: 32,
    padding: 6,
    borderRadius: 60,
    marginVertical: 4,
    elevation: 2
  },
  label: {
    fontSize: 22,
    color: 'black',
    textAlign: 'left',
  },
});
