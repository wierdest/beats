import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flexDirection: 'row',
    marginVertical: 12,
    padding: 12,
  },
  columnWrapper: {
		justifyContent: 'flex-start',
    gap: 12
	},


  
});